{{define "title"}}Index{{end}}

{{define "body"}}
	<h1>This is the index page</h1>
	<p>The bolt part of the execution took {{ .BoltDur }}</p>

	<div class="trending-lang-lists">
	{{ range .Langs }}
		{{ template "trending-lang" . }}
	{{ end }}
	</div>
	<p>This is period {{ .Period }}</p>
{{end}}
